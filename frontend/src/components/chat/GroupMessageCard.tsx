import type { Conversation } from '@/types/chat'

const GroupMessageCard = ({conversation} : {conversation: Conversation}) => {
  return (
    <div>GroupMessageCard</div>
  )
}

export default GroupMessageCard